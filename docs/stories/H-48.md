---
id: H-48
state: draft
surface: web
design: none
evidence: self
evidence_note: "Your shares with more than 50 grants, and a revoke that the holder refuses."
findings: "review-2: R2-008 (high), R2-013 (medium), R2-014 (medium)"
linear: ""
branch: ""
pr: ""
artifacts: []
blocked_reason: ""
owner: ""
job: ""
lease_expires: ""
updated: 2026-09-12
---
# H-48 — Your shares reports the wrong thing about what has ended

## Story
- A live share can be shown as ended because of a query limit, a refused revoke can lock the sheet,
  and the history of what ended lives in one browser.

## Context
From review 2.

**R2-008 — high — the 50-row grant cap marks live shares as ended.** `lib/arkiv.ts:228` caps the
query, and a sender past that cap sees live shares reported as ended. This is the worst kind of
wrong answer in this product: it tells someone access has stopped when it has not, so they stop
worrying about a link that still opens.

**R2-013 — medium — a rejected revoke locks the confirmation sheet.**
`app/(sender)/shares/page.tsx:113`. "End access now" is the one destructive control here, and a
sheet that cannot be dismissed or retried after a refusal leaves the sender unsure whether it
happened.

**R2-014 — medium — ended-share history exists only in one browser.**
`app/(sender)/shares/local-history.ts:23`. The same passkey on another device shows a different
history, which contradicts what sign-in tells the sender: the same face opens the same archive.

## What to build
- Page the grant query, or make the cap visible. A sender must never be told a live share ended.
  If the honest fix is "we cannot list more than N and we say so", that is acceptable and the
  guessing is not.
- A refused revoke returns the sheet to a state the sender can act from, and says what happened.
- Decide where ended-share history lives and make sign-in's promise true, or change the promise.
  Say which you chose and why in `## Choices` — this one is a design call, not only a bug.

## Evidence
A sender with more than 50 grants, and a holder that refuses a revoke. Both as tests that fail
before the fix.

## Review log
- review-2: R2-008, R2-013, R2-014

## Choices
- **R2-014 — kept ended-share history local, and changed the sign-in promise instead of the
  storage.** Making the promise literally true would need somewhere durable and cross-device to
  keep it — Arkiv itself, since that is the only address-scoped store this app has. A "revoked by
  you" record could be written there (the sender already signs the revoke). A "expired
  naturally" record could not: the whole of Arkiv's expiry story is that the entity — and every
  attribute on it — is gone once the block passes, by design, with no residual record and no
  cleanup job (see `lib/arkiv.ts`'s header comment). Building sync for only one of the two ended
  states would give a history that sometimes follows the sender and sometimes does not, with
  nothing on screen to say which — a worse promise than the one being fixed. `docs/stories/H-48.md`
  marks `design: none` for this story, so I changed the copy in `components/sign-in-screen.tsx`
  (both `BsnX6` desktop and `GdIlc` mobile variants) rather than the `.pen` design source: "the
  same face reopens your live shares... what's already ended stays on this device." That is true
  for both browsers and both platforms today. I did not touch `healthsend.pen` itself — editing it
  needs the pencil MCP tool against a running design app, which is design work this story's own
  frontmatter says is out of scope; the code is now the source of truth for this copy until a
  design pass reconciles the frame.
